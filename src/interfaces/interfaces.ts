export type Dimension = number | string;

export type LanguageType = 'de' | 'en' | 'es' | 'ja';

export type ThemeType = 'light' | 'dark';

export type Severity = 'success' | 'error' | 'info' | 'warning' | 'mention';

export enum CurrencyTypeEnum {
  USD = 'USD',
  ETH = 'ETH',
  BTC = 'BTC',
}
